/**
 * Type definitions for TemplateAppHeader
 * Add application-specific header types here as needed
 */

export interface TemplateAppHeaderProps {
  /**
   * Optional custom app name to display in header
   * Defaults to environment.appInfo.appName
   */
  appName?: string;
}
