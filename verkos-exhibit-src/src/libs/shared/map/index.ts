/**
 * Map Library Public API (Phase 6)
 *
 * Root entrypoint remains `@map` (`libs/shared/map/index.ts`) but the implementation
 * is now sourced from the new `src/public/**` structure.
 */
export * from './src/public';
